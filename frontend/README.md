# Frontend UI

- `index.html` contains the visible page structure.
- `styles.css` controls layout, colors, and responsive design.
- `app.js` coordinates UI actions and talks to the backend API.
- `../speech_to_text/` contains browser speech-recognition code used by the UI.
